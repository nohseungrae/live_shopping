import React from 'react';
import { ViewContainer } from './view_container';

export const ViewLayout = () => {
    return (
        <div className={'view'}>
            <ViewContainer />
        </div>
    );
};
